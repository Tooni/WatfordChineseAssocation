import { createTheme } from "@mantine/core";

export const theme = createTheme({
  primaryColor: "red",
  fontFamily:
    "Georgia,Garamond,Palatino,Times,Times New Roman,Arial,Helvetica,sans-serif",
  fontSizes: {
    xs: '14px',
    sm: '16px',
    md: '18px',
    lg: '20px',
    xl: '24px',
  },
});
